# Open Brain — connection credentials
# Copy this file to credentials.txt and fill in your values.
# credentials.txt is git-ignored and should never be committed.

# Supabase project URL
PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anon key (safe for client-side use; RLS is your security layer)
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# MCP Edge Function URL and access key (used by Claude / MCP clients only)
MCP_URL=https://your-project-ref.supabase.co/functions/v1/open-brain-mcp
MCP_KEY=your-mcp-key-here
